var download = require('download');
var path = require('path');


module.exports = function(urls, dest, opts) {

	urls = Array.isArray(urls) ? urls : [urls];

	urls = urls.filter(function() {



	});

};